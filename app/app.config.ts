export default defineAppConfig({
    ui: {
        input: {
            variants: {
                variant: {
                    outline:
                        "text-highlighted bg-[var(--input-bg)] ring ring-inset ring-accented",
                },
            },
        },
    },
});
